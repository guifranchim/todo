name: Build and Deploy to EKS

on:
  push:
    branches:
      - dev
      - main

env:
  AWS_REGION: us-east-2
  CLUSTER_NAME: todo-ua
  IMAGE_NAME_BACK: guifranchim/todolist-ua-back
  IMAGE_NAME_FRONT: guifranchim/todolist-ua-front
  TAG: latest

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: aws
    steps:
      - name: Checkout do Repositório
        uses: actions/checkout@v3

      # - name: Setup Docker Buildx
      #   uses: docker/setup-buildx-action@v2

      # - name: Login no Docker Hub
      #   uses: docker/login-action@v2
      #   with:
      #     username: ${{ secrets.DOCKERHUB_USERNAME }}
      #     password: ${{ secrets.DOCKERHUB_TOKEN }}

      # - name: Build e Push da Imagem do Backend
      #   uses: docker/build-push-action@v4
      #   with:
      #     context: ./backend
      #     file: ./backend/Dockerfile
      #     push: true
      #     tags: ${{ env.IMAGE_NAME_BACK }}:${{ env.TAG }}

      # - name: Build e Push da Imagem do Frontend
      #   uses: docker/build-push-action@v4
      #   with:
      #     context: .
      #     file: ./Dockerfile
      #     push: true
      #     tags: ${{ env.IMAGE_NAME_FRONT }}:${{ env.TAG }}

      - name: Configurar credenciais AWS
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-2

      - name: Deploy para EKS
        uses: giovannirossini/aws-eks-with-kubectl@v2.0.0
        with:
          aws-access-key-id:    ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region:           "us-east-2"
          cluster-name:         "todo-ua"
          kubectl-version:      "1.32"
          command: |
            if [[ "${GITHUB_REF##*/}" == "main" ]]; then
              kubectl apply -f infra/app.yaml -n production
            else
              kubectl apply -f infra/app.yaml -n stage
            fi